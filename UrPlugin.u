
class UrPlugin {
	var bot = nil;
	function init {
		var args = call.evalArgs;
		bot = args[0];
		// handle plugins with no arguments (pong)
		if (this.hasSlot("configure")) configure(args.tail);
	};
	var load = function() {};
	var unload = function() {};
};




requireFile("UrPlugin.u");


class UrParrot : UrPlugin {
	load = function () {
		at (bot.recv?("chan_msg", var captures)) {
			var chan = bot.getChan(captures["chan"]);
			chan.sendMessage(captures["nick"] + ": " + captures["msg"]);
                };
	};
};





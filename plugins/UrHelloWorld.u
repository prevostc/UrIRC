
requireFile("UrPlugin.u");

class UrHelloWorld : UrPlugin {
	var chan = nil;
	function configure(params) {
		chan = params[0];
		bot.addChan(chan);
	};
	load = function () {
		bot.getChan(chan).join;
		bot.getChan(chan).sendMessage("Hello World");
	};
};





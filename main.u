
requireFile("UrBot.u");
requireFile("plugins/UrHelloWorld.u");
requireFile("plugins/UrParrot.u");

// urbi necessary socket poll
every (500ms)
	Socket.poll,

var bot = UrBot.new("irc.iiens.net", 6667, "Urbot");
bot.connect;

UrHelloWorld.new(bot, "#urbot").load;
UrParrot.new(bot).load;

